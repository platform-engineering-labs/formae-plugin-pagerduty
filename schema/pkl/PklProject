amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "pagerduty"
  baseUri = "package://localhost/plugins/pagerduty/schema/pkl/pagerduty"
  version = "0.1.2"
  packageZipUrl = "https://localhost/plugins/pagerduty/schema/pkl/pagerduty@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.84.0"
  }
}
